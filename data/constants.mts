const characterTypeIds = {
	default: 0,
	runner: 1,
	collector: 2,
}

const validCharactersIds = Object.values(characterTypeIds)

export const constants = {
	characterTypeIds,
	validCharactersIds,
}

export default constants
