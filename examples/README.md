(if i had some) example code using node-celaria-server.

The examples will also use a custom campaign made specifically for node-celaria-server. I have designed these maps to be completable for beginners but there are enough routes for the experienced players. The maps can also be found on the Steam workshop. (if those are finished)
